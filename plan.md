# AI 产品面试题库筛选网页实施计划

## 1. 项目目标

把现有飞书文档中的 AI 产品面试题，转化成一个便于搜索、筛选、复习和持续维护的小网页。

网页需要同时解决三个问题：

1. **题目可筛选**：按公司、能力维度、业务场景、题型、轮次、频级、是否公司特异等字段组合筛选。
2. **相似题可归并**：以“母题”为主要展示对象，相似问法折叠在母题下面，避免重复阅读。
3. **数据可持续更新**：后续新增题目或调整标签时，不需要重新手改网页页面，只更新结构化数据并重新发布。

---

## 2. 推荐方案

### 2.1 总体架构

推荐采用：

> **React 前端网页 + JSON 结构化题库 + 自动校验/构建脚本 + 静态部署**

第一版不做后端服务，不使用数据库，也暂时不把飞书多维表格作为主数据源。

数据流如下：

```text
飞书文档（原始资料与长期归档）
        ↓ 首次转换 / 后续按需同步
questions.json（网页唯一读取的结构化底库）
        ↓ 校验、去重、生成索引
React 小网页
        ↓ 构建与部署
可访问的网页链接
```

### 2.2 为什么不推荐“单个纯 HTML 文件”

纯 HTML 可以实现最基础的展示，但不适合当前题库的复杂度：

- 需要多个组合筛选器、搜索、排序、折叠和状态管理。
- 后续可能增加收藏、浏览状态、公司视图和统计。
- 把数据直接写进 HTML 会导致数据与页面代码混在一起，每次新增题目都容易改坏页面。
- 题目数量继续增长后，单文件会越来越难维护。

因此，网页仍然是纯前端、静态部署，但工程上使用 React 组件拆分，而不是把所有逻辑写在一个 HTML 文件里。

### 2.3 为什么第一版不推荐飞书多维表格

多维表格适合多人录入和简单筛选，但不是当前场景的最佳最终界面：

- 母题、相似问法和来源属于层级结构，表格展示会比较拥挤。
- 组合筛选、折叠阅读、复习模式和公司专题视图，网页体验更好。
- 如果网页实时读取多维表格，需要额外处理数据绑定、权限、字段类型、加载状态和写入失败。
- 用户当前的主要诉求是“自己维护、快速筛选和复习”，JSON 更直接、可版本管理、可回滚。

多维表格可以作为未来的可选编辑后台，但不建议作为第一版的主数据源。

---

## 3. GitHub 仓库是否需要建立

### 3.1 结论

**推荐建立 GitHub 仓库，但不是网页运行的硬性前提。**

建立仓库的主要价值不是“才能上线”，而是：

- 记录每一次题目、标签和分类的修改。
- 修改错误时可以回滚。
- 通过 Pull Request 审核大规模分类或去重调整。
- 可配置 GitHub Actions，在 JSON 更新后自动校验、构建和部署。
- 后续换设备或让其他人协作时，不依赖某一台电脑上的文件。

### 3.2 建议阶段

- **方案确认和首版开发阶段**：可以先在当前工作区完成，不要求用户先建仓库。
- **首版网页验证可用后**：再将完整工程放入用户自己的 GitHub 仓库。
- **进入长期维护阶段**：启用自动校验和自动部署。

### 3.3 仓库建议结构

```text
ai-interview-question-bank/
├── plan.md
├── README.md
├── package.json
├── src/
│   ├── components/
│   ├── pages/
│   ├── lib/
│   └── types/
├── public/
│   └── data/
│       └── questions.json
├── scripts/
│   ├── validate-data.mjs
│   ├── build-index.mjs
│   └── import-lark-doc.mjs
├── schemas/
│   └── questions.schema.json
└── .github/
    └── workflows/
        └── build-and-deploy.yml
```

---

## 4. 数据存在哪里

### 4.1 第一版的唯一网页底库

第一版网页使用：

```text
public/data/questions.json
```

网页打开时加载该文件，然后在浏览器中完成搜索、筛选和排序。

这意味着：

- 页面代码不包含具体题目。
- 更新题目时不需要改 React 组件。
- JSON 可以独立校验、备份、比较差异和回滚。
- 网页构建产物中会同时包含 JSON，因此不需要额外数据库。

### 4.2 飞书文档的角色

现有飞书文档继续保留，但角色调整为：

- 原始资料归档。
- 临时收集新面经、图片 OCR 和散题。
- 阅读友好的母题说明文档。
- 必要时作为重新生成 JSON 的来源。

**网页不会在第一版中每次打开都实时读取飞书文档。**

原因是飞书文档正文是面向阅读的富文本，不是稳定的数据接口。标题、列表、标签文本发生小变化，都可能影响解析。把飞书文档直接当运行时数据库，会使网页容易因文档格式调整而失效。

---

## 5. JSON 数据模型

### 5.1 一道母题的建议结构

```json
{
  "id": "Q-0001",
  "title": "如何设计一套科学有效的 AI 评测体系？",
  "dimension": "评测体系与数据归因",
  "scenarios": ["通用", "RAG", "Agent"],
  "companies": ["百度", "腾讯", "字节"],
  "companyStyles": ["技术深挖", "方法论追问"],
  "rounds": ["一面", "二面"],
  "questionTypes": ["方法题", "技术理解题"],
  "frequency": "S",
  "companySpecific": false,
  "variants": [
    "评测集怎么建？",
    "如何保证评测集的代表性？",
    "为什么不直接使用公开 benchmark？",
    "人工标注和上线阈值如何设计？"
  ],
  "sources": [
    {
      "company": "百度",
      "round": "一面",
      "sourceType": "image-ocr"
    }
  ],
  "updatedAt": "2026-08-30"
}
```

### 5.2 为什么把字段拆开

标签不再写成长句，而是拆成固定枚举字段：

- `dimension`：单选，表示主能力维度。
- `scenarios`：多选，例如 RAG、AI 搜索、游戏 AI、内容生态。
- `companies`：多选，表示题目出现过的公司。
- `companyStyles`：多选，表示常见追问风格。
- `rounds`：多选，例如一面、二面、总监面、HR 面。
- `questionTypes`：多选，例如项目深挖、方法题、行为面、战略题。
- `frequency`：固定使用 S/A/B/C。
- `companySpecific`：布尔值，表示是否依赖特定公司业务。

网页只显示短标签 chip；字段的完整解释放在页面帮助区，不在每道题里重复。

### 5.3 稳定 ID 规则

每道母题使用稳定 ID，例如 `Q-0001`。

稳定 ID 很重要：

- 修改标题或标签时，依赖题目 ID 的链接和来源关系不会丢失。
- 合并母题时可以记录被合并题目的旧 ID。
- 后续接入多维表格或数据库时可以直接用作主键。
- 网页可以生成可分享的题目直达链接，例如 `?question=Q-0001`。

---

## 6. 飞书文档如何首次转成 JSON

### 6.1 首次迁移流程

1. 读取当前飞书文档正文。
2. 识别能力维度、业务场景、母题标题、标签和相似问法。
3. 将长标签拆成固定字段。
4. 为每个母题生成稳定 ID。
5. 输出 `questions.json`。
6. 使用 JSON Schema 和校验脚本检查：
   - 必填字段是否缺失。
   - 公司、轮次、频级是否使用合法枚举。
   - ID 是否重复。
   - 相似问法是否为空或重复。
   - 来源信息是否保留。
7. 人工抽查母题归并和公司标签。
8. 网页读取 JSON 并完成首版展示。

### 6.2 首次迁移的原则

- 飞书文档是输入，JSON 是运行时底库。
- 不根据字面完全一致去重，而是按底层考察能力合并母题。
- 每个母题保留代表性相似问法，不丢失公司的特殊问法。
- 不把飞书文档里的长标签原样复制到 JSON。

---

## 7. 后续如何更新 JSON

推荐提供三条更新路径，按使用频率逐步建设。

### 7.1 路径 A：网页内新增或编辑，导出 JSON

适合用户自己日常维护，建议在第一版就提供基础能力。

流程：

1. 用户在网页中点击“新增题目”或“编辑”。
2. 填写母题、公司、场景、轮次、题型和来源。
3. 网页进行字段校验。
4. 网页生成新的完整 `questions.json` 并下载。
5. 用下载的新文件替换仓库中的 `public/data/questions.json`。
6. 提交 GitHub 后自动构建和部署。

优点：不需要直接手写 JSON，操作直观。

限制：纯静态网页不能直接修改服务器上的文件，所以第一版采用“编辑后导出 JSON”，而不是假装已经保存到线上。

### 7.2 路径 B：直接编辑 JSON

适合一次性批量调整标签、来源或题目。

流程：

1. 在编辑器或 GitHub 网页上修改 `questions.json`。
2. 运行 `pnpm validate:data`。
3. 校验通过后提交。
4. 自动构建和部署。

为了避免手写出错，会同时提供：

- JSON Schema 自动提示。
- 枚举值校验。
- 重复 ID 和重复题目检查。
- 格式化脚本。

### 7.3 路径 C：继续往飞书文档添加，再执行同步脚本

适合用户继续沿用当前“把图片或文字发到飞书文档”的收集方式。

流程：

1. 在飞书文档中新增“待入库”区域。
2. 将新题目或图片 OCR 内容放入该区域。
3. 运行 `import-lark-doc.mjs` 同步工具。
4. 工具只读取待入库部分，并生成“待确认变更清单”。
5. 用户确认以下内容：
   - 新建母题还是并入现有母题。
   - 公司、场景、轮次等标签。
   - 是否补充或调整现有相似问法和来源。
6. 确认后更新 `questions.json`。
7. 校验、提交、自动部署。

同步工具不应该无确认地覆盖现有题库，尤其不能自动改变人工确认过的母题归并和标签。

---

## 8. 答案能力暂不纳入当前范围

当前版本只管理题目、相似问法、标签和来源，不创建回答框架、1 分钟版、2 分钟版、个人案例或追问答案字段。

这样可以先把题库数据治理和筛选体验做好，避免首版范围过大。如果未来需要补充答案，可以在不破坏现有题目 ID 的前提下扩展 JSON Schema 和网页组件。

---

## 9. 网页功能范围

### 9.1 第一版必须完成

- 关键词搜索：母题和相似问法均可搜索。
- 多选筛选：公司、能力维度、场景、题型、轮次、频级。
- 布尔筛选：是否公司特异。
- 筛选结果计数和一键清空。
- 母题卡片展示，标签使用短 chip。
- 相似问法折叠展开。
- 排序：频级、更新时间、题号。
- URL 保存当前筛选条件，刷新或分享后仍保留。
- 新增/编辑题目表单。
- 导出完整 JSON 和当前筛选结果。
- JSON 导入预览、校验与确认覆盖。
- 手机、平板、桌面端可用。

### 9.2 第二版可选功能

- 收藏、已准备、需复习等个人状态。
- 按公司生成专题复习视图。
- 高频母题速背模式。
- 重复题目检测与合并建议。
- 标签和题目数量统计。
- 题目直达链接。

### 9.3 暂不做

- 用户系统和权限管理。
- 在线数据库。
- 多人实时协作编辑。
- 自动无审核地同步飞书文档。
- 复杂后端服务。

---

## 10. 前端技术方案

### 10.1 技术栈

推荐使用：

- React + TypeScript
- Vite / 现有网页应用脚手架
- Tailwind CSS
- shadcn/ui 组件
- Lucide 图标
- 浏览器端 JSON 加载和筛选

不需要后端，也不需要数据库。

### 10.2 主要组件

```text
App
├── Header（题库说明、总数、更新时间）
├── SearchBar（全文搜索）
├── FilterPanel
│   ├── CompanyFilter
│   ├── DimensionFilter
│   ├── ScenarioFilter
│   ├── QuestionTypeFilter
│   ├── RoundFilter
│   └── FrequencyFilter
├── ActiveFilterBar（当前筛选条件）
├── QuestionList
│   └── QuestionCard
│       ├── TagChips
│       ├── VariantList
│       └── AnswerPanel
├── QuestionEditor
├── JsonImportDialog
└── Footer
```

### 10.3 数据读取方式

网页启动后请求：

```text
./data/questions.json
```

数据加载失败时显示明确错误，不让页面白屏。

所有筛选和搜索在浏览器本地完成。以当前题库规模，即使扩展到数千题，也不需要后端搜索服务。

---

## 11. 实时更新的准确含义

### 11.1 第一版的更新方式

第一版是静态网页，更新链路是：

```text
更新 JSON → 提交仓库 → 自动构建 → 自动部署 → 网页刷新后看到新数据
```

这属于**发布后自动更新**，不是数据库级的实时写入。

如果配置 GitHub Actions，用户提交 JSON 后，网页通常在几分钟内更新，不需要人工改页面代码。

### 11.2 如果未来要求“保存后立即线上生效”

需要增加在线数据源，推荐优先级：

1. 飞书多维表格作为编辑后台，网页实时读取。
2. 增加轻量后端和数据库。
3. 使用 GitHub API 由网页直接提交变更。

这些方案会引入额外的权限、鉴权、写入失败和冲突处理，不建议第一版就做。

---

## 12. 部署方案

### 12.1 首版部署

首版开发完成后构建为静态资源并部署，交付一个可访问链接。

### 12.2 GitHub 后续自动部署

建立仓库后建议配置：

1. 修改并提交 `questions.json`。
2. GitHub Actions 自动执行数据校验。
3. 校验通过后执行前端构建。
4. 自动发布新版本。
5. 校验失败则阻止发布，并提示具体字段错误。

### 12.3 缓存策略

为了避免 JSON 更新后浏览器仍显示旧内容：

- `questions.json` 使用短缓存或不缓存策略。
- 页面显示题库版本号和 `updatedAt`。
- 必要时给数据 URL 添加版本参数。

---

## 13. 实施阶段

### 阶段 0：确认方案

- 用户确认本计划。
- 确认是否需要首版网页编辑和 JSON 导入/导出。
- 确认是否在首版完成后建立 GitHub 仓库。

### 阶段 1：迁移与数据治理

- 从当前飞书文档提取母题和相似问法。
- 定义枚举字典与 JSON Schema。
- 生成稳定 ID。
- 输出并校验首版 `questions.json`。
- 抽查公司和轮次标签。

### 阶段 2：网页开发

- 初始化 React 前端工程。
- 实现搜索、筛选、排序和卡片展示。
- 实现相似问法折叠。
- 实现 URL 筛选状态。
- 实现导入、导出和编辑表单。

### 阶段 3：测试与部署

- 验证数据完整性和筛选准确性。
- 验证宽屏、平板、手机布局。
- 构建并部署网页。
- 交付访问链接。

### 阶段 4：维护自动化

- 建立 GitHub 仓库。
- 配置数据校验和自动部署。
- 增加飞书“待入库”同步工具。
- 编写题目新增、标签更新和回滚说明。

---

## 14. 关键决策与建议

### 14.1 本计划的最终建议

1. **需要建立 GitHub 仓库，但可以在首版验证后再建立。**
2. **网页不是单个纯 HTML 文件，而是 React 静态前端应用。**
3. **第一版不需要后端，不需要数据库。**
4. **`questions.json` 是网页唯一读取的结构化底库。**
5. **飞书文档保留为原始资料和待入库入口，不直接作为网页运行时数据库。**
6. **日常维护优先使用网页编辑并导出 JSON，或直接编辑 JSON 后提交。**
7. **飞书文档同步采用“生成变更清单 → 人工确认 → 更新 JSON”，不自动改变已确认的归并和标签。**
8. **GitHub Actions 负责校验、构建和自动发布，达到几分钟内更新。**

### 14.2 首版开发范围建议

首版直接实现：

- 完整题库数据迁移。
- 所有主要筛选器。
- 搜索、排序和母题折叠。
- 来源与标签展示。
- 新增/编辑题目。
- JSON 导入、导出和校验。
- 部署访问链接。

这样既能立即满足筛选需求，也能保证后续新增题目或调整标签时，不需要重新开发网页。

---

## 15. 验收标准

首版完成后应满足：

- 当前题库中的母题均已进入 JSON，并有稳定 ID。
- 公司、能力维度、场景、题型、轮次和频级均可组合筛选。
- 筛选结果与 JSON 数据一致。
- 相似问法不会作为重复母题平铺展示。
- 每道母题均保留相似问法、结构化标签和来源信息。
- 用户可以不修改代码，通过网页表单维护数据并导出 JSON。
- 导入错误 JSON 时有可理解的错误提示。
- 更新 JSON 后不修改页面代码即可发布新版本。
- 桌面端和手机端均可正常使用。
- 项目具备迁移到 GitHub 自动维护的完整路径。

---

## 16. 已确认的分类与交互修正规则

以下规则已由用户确认，作为题库后续维护、导入、校验、展示与部署的固定约束；保留前述总体方案不变：

1. **TRAE 归属字节跳动**：TRAE 是字节跳动产品。所有 TRAE 相关题目以及其他原本属于字节跳动的题目，公司标签统一使用全称“字节跳动”，不得使用简称“字节”；TRAE 可继续出现在母题标题、相似问法、来源或产品信息中，但“字节”和“TRAE”均不得作为独立公司筛选项。
2. **美图母题归类**：母题“美图 / 影像方向：AIGC 修图或写真产品如何做差异化与体验优化？”统一改为“美图方向：AIGC 修图或写真产品如何做差异化与体验优化？”，公司分类只保留“美图”；“影像”“影像方向”不得作为公司分类或筛选项。
3. **旧方向字段归并**：“办公 Agent 方向”“企业 ToB”“ToB”“教育方向”不再作为业务场景或筛选字段。历史题目中的这些分类统一归并为“通用”；若题目原本已包含“通用”，则删除旧字段并去重。
4. **防止旧分类复活**：`questions.json`、动态筛选项、浏览器 JSON 导入校验、数据校验脚本和受控迁移脚本均须执行上述归类规则。旧 URL 筛选参数和旧浏览器缓存不得让已删除分类重新出现在页面中。
5. **桌面端滚动与容错**：在桌面端且视口高度足够时，页面固定在可用视口高度内，左侧筛选栏与右侧题目内容分别使用独立纵向滚动，鼠标位于对应区域时均可滚动且互不锁死；`html`、`body`、`#root`、App 根容器以及 flex/grid 子项必须正确设置高度链路与 `min-height: 0`。移动端必须恢复 `body` 自然单列滚动，不得被 `overflow: hidden` 锁住；桌面端窗口高度不足时也必须降级为页面自然滚动，确保标题、筛选、题目与底部操作全部可访问，不得以维持双栏滚动为代价裁切内容。
6. **首页定位文案**：首页主文案明确强调 AI 产品经理面试题，使用“把零散的 AI 产品经理面试题，整理成可复习的母题。”。
7. **数据范围**：当前及后续数据修正不得新增答案字段；题库仍只管理母题、相似问法、分类标签与来源。
8. **回归要求**：每次涉及分类或布局的更新均须执行数据校验、Lint、TypeScript 编译、构建，并对桌面端独立滚动和移动端页面进行视觉验证后再部署。
